# Origin of the chat API. The socket connects to this host's root and REST calls
# go to <origin>/api. Defaults to the hosted service when unset.
NEXT_PUBLIC_API_ORIGIN=https://frontend-task-chatapp.onrender.com
